[package]
name = "dfunc"
version = "0.1.0"
edition = "2021"

[features]
enzyme = ["autodiff"]

[dependencies]
num-traits = {version = "0.2.17", default-features = false}
autodiff = { path = "/home/jed/src/rust-enzyme/library/autodiff", optional = true }
